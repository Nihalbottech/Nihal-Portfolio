rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Portfolio content: anyone can read, only logged-in users can write
    match /portfolio/{document} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}